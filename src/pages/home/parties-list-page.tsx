import { Container, Typography } from '@mui/material';

export const PartiesListPage = () => {
  return (
    <Container maxWidth="xl">
      <Typography component="h1">PartiesListPage</Typography>
    </Container>
  );
};
